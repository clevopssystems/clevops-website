import type { Metadata } from "next";
import WorkPage from "./_content";

export const metadata: Metadata = {
  title: "Our Work",
  description:
    "See how ClevOps transformed a residential cleaning service from an invisible online presence into a premium, lead-generating brand. Real results, real strategy.",
};

export default WorkPage;
